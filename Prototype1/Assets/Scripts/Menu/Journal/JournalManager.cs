/*
 * Avery
 */
using UnityEngine;
using TMPro;

public class JournalManager : MonoBehaviour
{
    public JournalItem[] collectibleList, characterList, enemyList;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}